CREATE OR REPLACE FUNCTION GET_CURRENT_PEOPLE RETURN NUMBER IS

LV_TOTAL_PEOPLE NUMBER;

BEGIN

SELECT COUNT(*) INTO LV_TOTAL_PEOPLE 
FROM CAFETERIA_LOG
WHERE EXIT_TIME IS NULL;

RETURN LV_TOTAL_PEOPLE;

END;